package view.inputs;

public class InputFactory {

   /* public CmdInput createInput(InputTypes inputType, String prompt) {
	
    }
    
    public CmdInput createInput(InputTypes inputType, String prompt, int maxInt) {
	switch (inputType) {
	    case INT:
		return new IntInput(prompt);
	    case LIMITED_INT:
		return new LmtIntInput(prompt, maxInt);
	    case WORD:
		return new WordInput(prompt);
	    case ENTER:
		return new EnterInput();
	    case BOOLEAN:
		return new BooleanInput(prompt);
	    default:
		return null;

	}

    }*/
}
